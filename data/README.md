# Data Layout

- `raw/`: source datasets copied into the repository workspace for local runs.
- `processed/`: generated path and k-best datasets derived from the raw inputs.

These folders are ignored by Git so local experiments do not pollute the repository history.
